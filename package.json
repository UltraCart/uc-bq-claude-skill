{
  "name": "@ultracart/bq-skill",
  "version": "0.1.11",
  "description": "UltraCart BigQuery reporting skill for Claude Code — CLI + skill prompt for creating and replaying e-commerce reports",
  "main": "dist/cli.js",
  "bin": {
    "uc-bq": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/cli.js",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test \"dist-test/test/**/*.test.js\"",
    "test:watch": "npm run build:test && node --test --watch \"dist-test/test/**/*.test.js\"",
    "test:coverage": "npm run build:test && node --test --experimental-test-coverage \"dist-test/test/**/*.test.js\"",
    "test:tz": "TZ=UTC npm test && TZ=America/New_York npm test && TZ=Asia/Tokyo npm test && TZ=Pacific/Auckland npm test",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "ultracart",
    "bigquery",
    "claude-code",
    "reporting",
    "echarts",
    "e-commerce"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/UltraCart/uc-bq-claude-skill"
  },
  "author": "UltraCart",
  "license": "Apache-2.0",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@aws-sdk/client-bedrock-runtime": "^3.0.0",
    "@google-cloud/bigquery": "^8.0.0",
    "@google/generative-ai": "^0.24.0",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0",
    "commander": "^15.0.0",
    "js-yaml": "^5.2.3",
    "md-to-pdf": "^5.2.5",
    "openai": "^7.4.0",
    "puppeteer": "^25.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "files": [
    "dist/",
    "schemas/",
    "templates/",
    "skill/"
  ],
  "volta": {
    "node": "24.13.0"
  }
}
